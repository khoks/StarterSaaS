{
  "name": "@starter-saas/saga",
  "version": "0.0.0",
  "private": true,
  "description": "Saga primitives for StarterSaaS — TS-coded state machines + compensation registry (per D-45 / ADR-0005). Event-driven choreography over @starter-saas/event-bus.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0"
  },
  "dependencies": {
    "@starter-saas/event-bus": "*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.0.0"
  }
}
