{
  "name": "@starter-saas/tenancy",
  "version": "0.0.0",
  "private": true,
  "description": "Tenancy subsystem for StarterSaaS — schema-per-tenant infrastructure (per ADR-0004): platform.tenants registry, platform.tenant_migrations, platform.tenant_archive_log, platform.saga_instances + DrizzleSagaStore. The 9-step provisioning saga + withTenants() wrapper land in subsequent sub-PRs of STORY-014.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./db/schema": "./src/db/schema.ts"
  },
  "files": [
    "src",
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0"
  },
  "dependencies": {
    "@starter-saas/saga": "*",
    "drizzle-orm": "^0.36.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.0.0"
  }
}
