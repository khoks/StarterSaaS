{
  "name": "@starter-saas/observability",
  "version": "0.0.0",
  "private": true,
  "description": "Observability foundation for StarterSaaS — OTel SDK + GenAI semconv + Pino logger + PII redactor + adaptive sampler + tenant-context propagation per ADR-0006. Cloud-native backends + Langfuse integration land in subsequent sub-PRs of STORY-019.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@opentelemetry/api-logs": "0.55.0",
    "@opentelemetry/resources": "1.28.0",
    "@opentelemetry/sdk-logs": "0.55.0",
    "@opentelemetry/sdk-node": "0.55.0",
    "@opentelemetry/sdk-trace-base": "1.28.0",
    "@opentelemetry/sdk-trace-node": "1.28.0",
    "@opentelemetry/semantic-conventions": "1.28.0",
    "pino": "^9.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.0.0"
  }
}
