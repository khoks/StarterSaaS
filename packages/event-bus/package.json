{
  "name": "@starter-saas/event-bus",
  "version": "0.0.0",
  "private": true,
  "description": "Event bus for StarterSaaS — Kafka-shaped contract surface (per D-45 / ADR-0005). Ships an InMemoryEventBus adapter for tests + saga-driven flows. pg-outbox adapter lands in STORY-017; native Kafka adapter is the v1 target.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.0.0"
  }
}
