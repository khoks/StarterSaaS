# Decisions log

> Lighter-weight than ADRs. ADRs are full architectural decisions with context / alternatives / consequences. The decisions log is a chronological list of *every* meaningful decision — product, process, architectural, tooling — captured by the `harvest-knowledge` skill. ADR-grade decisions get cross-linked here AND get a full ADR file.
>
> Format: `D-NNN — date — area — title — one-line rationale — link (if any)`.

---

## Phase A bootstrap (this session)

| ID | Date | Area | Decision | Rationale | Link |
|---|---|---|---|---|---|
| D-1 | 2026-04-25 | repo-location | Project lives at `D:\DEV\ClaudeProjects\StarterSaaS` | Sibling to LearnPro; independent project; no shared monorepo | — |
| D-2 | 2026-04-25 | naming | Project name is `StarterSaaS` | User-confirmed; "happy-cocke" was a speech-to-text artifact and discarded | — |
| D-3 | 2026-04-25 | license | License is MIT | Maximum permissiveness for white-label adoption; adoption > protection | [ADR-0001](../architecture/ADR-0001-license-mit.md) |
| D-4 | 2026-04-25 | git | Default branch is `main` | GitHub default; will be PR-protected after Phase A | — |
| D-5 | 2026-04-25 | github | Remote is private repo `khoks/StarterSaaS` via `gh repo create` | Visibility flips to public when MVP-1 ships | — |
| D-6 | 2026-04-25 | tooling | Skill set: copy + adapt LearnPro's `harvest-knowledge` + `work-tracking`; add NEW `auto-pr` skill | Three-skill chain in Stop hook; preserves the proven prose+tracking flow and adds git automation | — |
| D-7 | 2026-04-25 | workflow | Auto-PR model: branch-per-session → push → `gh pr create` → `gh pr merge --auto --squash` | Keeps `main` always-green; PR queue is the audit trail | — |
| D-8 | 2026-04-25 | collaboration | Heavy upfront PM + engineer discussion on design / requirements / algorithms / tech stack BEFORE work; auto-merge after agreement | User explicitly requested. Auto-PR is a tool for landing already-agreed work, NOT a substitute for the discussion | — |
| D-9 | 2026-04-25 | scope | MVP-1 is a thin vertical slice end-to-end (~6 subsystems). Exact 6 chosen during Phase B/C grooming | Demonstrates every layer is reachable end-to-end, not that each subsystem exists in isolation | — |
| D-10 | 2026-04-25 | git | Bootstrap commit lands directly on `main` (no PR). After that, `auto-pr` takes over | Bootstrap *is* the PR-able infrastructure; can't PR before it exists | — |
| D-11 | 2026-04-25 | docs | Verbatim user vision goes into `docs/vision/RAW_VISION.md`. Transcription artifacts noted in a footnote, NOT edited inline | Body is source-of-truth for intent; editing inline would lose the user's framing | — |

---

## Phase B grooming

| ID | Date | Area | Decision | Rationale | Link |
|---|---|---|---|---|---|
| D-12 | 2026-04-27 | workflow | Phase B grooming runs as a single multi-session sweep through STORY-008 → STORY-012, with full PM treatment per open question (industry/competitive context + recommendation + tradeoffs + explicit invitation for user innovation + novelty analysis if user proposes one) | User explicitly chose this scope and cadence over piecemeal grooming or compact Q&A; instantiates D-8 for Phase B specifically; ensures the algorithmic-innovation probe `RAW_VISION.md` called for | — |
| D-13 | 2026-04-27 | persona | Primary persona is the **founder's first engineer** at a 1-5 person company. Secondary: solo founder (v1 "lite profile" — fewer layers enabled). Tertiary: small-team CTO inheriting a codebase where the first engineer already adopted the kit (lifecycle continuity) | This persona has authority + urgency + technical chops + felt-pain at once — the rare combination that produces deliberate, high-conversion adoption rather than churn. Whitespace in OSS-kit positioning: Supabase / Pocketbase / Appwrite / Firebase target founders generically, AWS / GCP target platform teams, nobody targets first engineers. Lifecycle continuity means today's first engineer is tomorrow's small-team CTO, so adoption happens earlier and grows with the company. Implies first-class migration-path support (engineer inherits existing code, not greenfield) — filed as a recommended addition | [GROOMED_FEATURES § Persona](../vision/GROOMED_FEATURES.md#persona) |
| D-14 | 2026-04-27 | workflow | Assistant is authorized to use `gh pr merge --admin --squash` to land **doc-only and tracking-only PRs** (housekeeping / grooming / decisions / ADRs) without waiting for a manual review approval. Branch protection on `main` stays strict (1 review required, markdown-lint required, linear history, no force-push). Source-code PRs (once Phase D begins) DO NOT get this override and must follow the normal review flow | User's solo-dev workflow: strict policy is right for code, but housekeeping PRs that only touch `docs/`, `project/`, `.claude/` would otherwise pile up indefinitely. Repo-level `allow_auto_merge` stays `false` — this is a deliberate per-PR escape hatch, not a blanket auto-merge. Scope discipline: the moment any PR touches `apps/`, `packages/`, `services/`, `infra/`, or `scripts/`, the override does not apply | — |
| D-15 | 2026-04-28 | differentiator + vision | StarterSaaS's headline differentiator (and core vision): **"AI-first, production-grade SaaS platform — 30+ integrated layers you will eventually need, white-labelable, one-command deployable."** Combines breadth (A) + production-grade (B) + one-command deploy (C) framings with a new dimension — **AI-first** — added by the user. Five named AI subsystems anchor the AI-first claim: (1) Event-driven Customer Profile Builder + AI-Native Stores, (2) Context-Resolving Query Service, (3) Agent Platform + Omnichannel Orchestrator with 6 registries, (4) LLM Gateway + Safety + Model Hub + Eval/Cost (foundational; MVP-1), (5) AI Coworker Platform for Internal Ops. CLAUDE.md mission updated; AI subsystems filed in NOVEL_IDEAS + RECOMMENDED_ADDITIONS | Whitespace in OSS positioning — no major OSS kit (Supabase / Pocketbase / Appwrite / Firebase / Strapi) is positioned AI-first. 2026 timing: every founder asks "how do I add AI?" and we answer before they ask. Persona fit: the founder's first engineer (D-13) is increasingly tasked with making the product AI-powered. Implies AI/ML platform moves from v2 (deferred) to MVP-1 candidate (foundational AI Subsystem 4 + subset of 3), expanding STORY-012 scope. Implies subscribe-to-upstream becomes more critical (Q3) because AI models/patterns evolve weekly | [GROOMED_FEATURES § Differentiator](../vision/GROOMED_FEATURES.md#differentiator) |
| D-16 | 2026-04-28 | kit-promise | Kit-promise model: **hybrid** — subscribe-to-upstream for the 30+ platform layers + 5 AI subsystems + adapter interfaces + deploy script; fork-once for white-label brand layer + product-specific business logic + custom UI screens. Mechanism: **packages + thin shell** — kit code ships as versioned packages, user repo is a thin shell importing them; adapter interfaces are package-exported contracts; user provides custom adapter implementations in shell. Concrete package manager TBD in STORY-010 (tech stack) | Persona D-13 (founder's first engineer) doesn't want to maintain platform code; subscribe-to-upstream matches their pain. AI-first D-15 means the 5 AI subsystems MUST be subscribable since models / prompts / safety techniques evolve weekly; pure fork-once would leave forks 4-5 model generations stale within 6 months. Pure subscribe-to-upstream removes the customization escape hatch for unusual product needs — the thin shell IS the escape hatch (vendor a package and customize when needed, accepting maintenance burden until you un-vendor). Pattern is well-validated in production by Saleor / Medusa / Drupal | [GROOMED_FEATURES § Kit-promise](../vision/GROOMED_FEATURES.md#kit-promise-model) |
| D-17 | 2026-04-28 | feature-scope | **AI-assisted upstream merge** confirmed as a first-class kit feature anchoring the AI-first + subscribe-to-upstream story. Mechanism: an agent detects upstream package updates → examines user customizations via adapter usage signatures → proposes a merge plan minimizing friction → tests the proposed merge against the user's test suite → surfaces conflicts to a human only when AI confidence is low. Implementation lives at the intersection of AI Subsystem 5 (Internal Ops Agent) and the kit-promise mechanism. Phase fit: **MVP-1** for the basic version (detect + propose + test); v1 for the full version (confidence scoring + autonomous-mode toggle). Implies AI Subsystem 5 needs a "lite" boundary — full Subsystem 5 is v2, but the merge-helper subset ships earlier | Combines two competitive moats — AI-first credibility + subscribe-to-upstream practicality — into one feature: "your kit upgrades itself." Direct response to "first engineer doesn't want to maintain platform code" pain (D-13) — even staying current is automated. No OSS kit ships "AI agent that helps you upgrade" as a first-class feature; component mechanisms (Dependabot / Renovate, Devin-style agents, LLM merge POCs) exist but the kit-aware-with-adapter-signatures + confidence-scored-escalation + integrated-as-first-class composition is novel. STORY-009 will design the ADR | [NOVEL_IDEAS — AI-assisted upstream merge](../vision/NOVEL_IDEAS.md) |
| D-18 | 2026-04-28 | site-topology | Pre-auth marketing site placement: **sibling repo** (`StarterSaaS-marketing` or per-adopter equivalent) as default; **monorepo with separate deploys** documented as a config-time alternative for users who prefer one-repo simplicity. Brand assets ship as a third package (`@starter-saas/brand`) by default, with opt-out for users who want manual duplication. Marketing site contains: hero / landing, pricing, blog, support documentation, contact, terms / privacy. Post-auth in-app help is separate (lives with the app). API / developer-portal docs deferred to STORY-009 — likely with the app for code-sync, but consumable via the marketing site | Persona D-13 (founder's first engineer) wants the app codebase clean. Marketing has fundamentally different deploy cadence, editor population, optimization target (SEO), and risk profile than the app — mixing them creates friction in every dimension. Pattern is industry-standard (Supabase / Vercel / Stripe / Pocketbase all separate). Sibling repo preserves D-15 (one-command deployable: deploy script `git clone`s both) and D-16 (each surface is independently subscribable). Monorepo alternative serves the secondary persona (solo founder) who prefers one-repo simplicity | [GROOMED_FEATURES § Site topology](../vision/GROOMED_FEATURES.md#site-topology) |
| D-19 | 2026-04-28 | site-topology | **Status page is separate** — third surface beyond app + marketing. Default: separate repo / managed service (Statuspage / Instatus / similar adapter). Lives at `status.<adopter-domain>`. Integrates with the kit's observability subsystem (incident management triggers status page updates via adapter) | Status page has its own deploy cadence, its own audience (customers + integrators), its own uptime story (must stay up when the app goes down — separate hosting is mandatory). Same logic as marketing-site separation but stricter on the uptime requirement. Adapter pattern preserves D-16 (subscribe-to-upstream) | [GROOMED_FEATURES § Site topology](../vision/GROOMED_FEATURES.md#site-topology) |
| D-20 | 2026-04-28 | white-label | White-label mechanism: **layered** — config-driven primary (90% of adopters), adapter overrides (8%), plugin extensions (2%). Each layer is the escape hatch for the previous. Config entry point: `starter.config.ts` (TypeScript with full type definitions for autocomplete + edit-time errors). Adapter pattern from D-16 supplies layer 2. Plugin extension points design is its own ADR (STORY-009). Code-gen explicitly rejected as a customization mechanism (used only for the initial scaffolding step `npx create-starter-saas`, which produces a minimal thin shell — not a customization vector) because code-gen breaks D-16 and D-17 | Layered approach matches industry-best (Strapi, Saleor, Next-auth all do this). Compatible with all locked decisions: D-13 fast-demo (edit config, deploy, done), D-16 subscribe-upstream (config is forward-compat), D-17 AI-assisted merge (config + adapter customizations have clear boundaries), D-18 brand-as-package (brand imported via config). Code-gen would have broken D-16 / D-17 — a hard "no" | [GROOMED_FEATURES § White-label mechanism](../vision/GROOMED_FEATURES.md#white-label-mechanism) |
| D-21 | 2026-04-28 | repo-topology | Kit development repo topology: **monorepo with workspaces**. One `khoks/StarterSaaS` repo containing `packages/<capability>/` workspaces (each publishing its own `@starter-saas/<capability>` package independently) + `apps/starter/` (the thin-shell template) + `tools/` (build / release / deploy scripts) + `docs/`. Each package has its own version, changelog, semver track. Final package-manager + monorepo-tooling pick (pnpm / yarn / npm; Turborepo / Nx / Bazel; etc.) deferred to STORY-010. **Publishing topology** stays polyrepo-shaped per D-16 — adopters install packages independently, no awareness of our internal monorepo | At our scale (1 active dev currently, modest contributor base), true polyrepo (one git repo per capability, ~40 repos) creates severe overhead: 40 CI configs, 40 branch-protection setups, 40x admin-override authorizations, coordinated cross-repo PRs for adapter contract changes, impossible atomic refactors, plugin discoverability problems, skills-replication burden. Industry-standard for OSS kits — Saleor, Medusa, Strapi, Vercel, Stripe internal, Shopify, Cloudflare workers SDKs, Next.js, Tanstack all ship monorepo-with-workspaces. Decision can be revisited if/when contributor count and capability ownership warrant per-repo extraction (one-way migration: easy to split later, near-impossible to un-split) | [GROOMED_FEATURES § Site topology](../vision/GROOMED_FEATURES.md#site-topology) |
| D-22 | 2026-04-28 | feature-scope | **AI-assisted config generation** confirmed as MVP-1 feature. First engineer describes the SaaS in natural language ("tutoring marketplace, need auth + payments + RBAC for teachers/students/admins, email notifications, basic analytics"); AI Subsystem 5 (Internal Ops Agent) generates a starting `starter.config.ts` + suggests adapter picks + scaffolds the thin-shell folder layout. Day-1 demo time drops from ~30 minutes to ~5. Lives at the intersection of AI Subsystem 5 (lite, MVP-1 subset per D-17 precedent) and the white-label config layer (D-20 layer 1). Trust-but-verify: engineer reviews + corrects AI output before applying — guardrail against hallucinated adapter picks | Pulls AI-first credibility into the very first user interaction with the kit. Pairs with D-13 (founder's first engineer must demo to founder fast) — compresses first hour to first minutes. Pattern of NL → config exists in fragments (Yeoman, create-* scripts, Vercel templates, AI dev assistants like Cursor / Devin) but no OSS kit ships kit-aware NL config generation that understands a fixed schema, suggests appropriate adapters, scaffolds folder layout, AND integrates with the white-label mechanism — that composition is novel | [NOVEL_IDEAS — AI-assisted config generation](../vision/NOVEL_IDEAS.md) |
| D-23 | 2026-04-28 | feature-scope | **AI-validated plugin compatibility** confirmed as MVP-1 feature. When a user writes a plugin against extension points, AI Subsystem 5 simulates upcoming kit upgrades against the plugin's hook signatures and surfaces likely breakage *before* the user merges an upstream update. Same agent vantage as D-17 (AI-assisted merge), different surface (D-17 looks at shell customizations; D-23 looks at plugins). Implies plugin extension-points must be machine-readable (good discipline anyway) | Removes the "plugin maintenance tax" that has historically choked plugin ecosystems (WordPress, Drupal). Encourages user-authored plugins (engineer can write a plugin without fearing silent break on upgrade). Combines with D-17 to make the entire subscribe-to-upstream story actually work, not theoretically work. Component mechanisms exist (TypeScript compat checking, semver enforcement, dependency analysis) but AI-driven plugin-vs-kit-version simulation as a first-class kit feature isn't documented in any OSS kit | [NOVEL_IDEAS — AI-validated plugin compatibility](../vision/NOVEL_IDEAS.md) |
| D-24 | 2026-05-02 | tech-stack | Backend stack: **TypeScript + Node.js + Fastify** on a **modular monolith** runtime model for MVP-1 (one process per service-shape, packaged as workspace packages within the monorepo per D-21). Node for BOTH production and local dev — Bun explicitly excluded (re-evaluate in v1+). MVP-1 ships pure TS — no Go / Rust / Python in the codebase. Polyglot escape hatch documented in D-26 for v1+ extraction of perf-critical subsystems with clear contracts. ADR-0002 to be written at STORY-010 closure | Persona D-13 (founder's first engineer): TS is the language they're most likely to know on day 1. AI-first D-15: TS has best-in-class AI SDK support (Vercel ai-sdk + Anthropic / OpenAI / AWS provider SDKs all first-class). Subscribe-to-upstream D-16: npm + pnpm is the most mature OSS package ecosystem. Monorepo D-21: Turborepo / Nx / pnpm workspaces are best-in-class. AI-validated plugin compat D-23: structural type system makes machine-readable extension-point specs natural. Full-stack consistency: same language as the frontend, lowering context-switch tax. Node over Bun: production maturity matters; Bun is promising but still maturing for prod-grade observability + stable hosting | — (ADR-0002 pending STORY-010 closure) |
| D-25 | 2026-05-02 | coding-standard | **TypeScript strict mode** (`"strict": true` in `tsconfig.json` for every package) AND **Zod schemas on every public boundary**: HTTP route inputs/outputs (Fastify-Zod plugin or equivalent), plugin extension points, event-bus message schemas, adapter interface contracts, `starter.config.ts` schema, AI agent input/output contracts. Internal-only code is allowed structural TS types without Zod. Shared between kit packages and adopter shell (the contract surface IS the boundary). ESLint + Prettier specifics deferred to STORY-010 closure / first PR landing source code | Strict mode + Zod everywhere is the coding-standards backbone of D-23 (AI-validated plugin compat) — an AI agent can only reason about extension points if those points have machine-readable shapes. Same logic for D-22 (AI-assisted config gen) — the config schema must be inspectable. Internal types are TS-only because Zod has runtime cost; only validate where data crosses a trust boundary (per existing CLAUDE.md "Validate at boundaries, trust internal code" rule, now made stack-specific). Pattern industry-validated by tRPC, Hono, Fastify-Zod, Effect Schema | — |
| D-26 | 2026-05-02 | polyglot-rule | **Polyglot escape-hatch rule** (governs when a subsystem earns extraction to Go / Rust / other in v1+): all THREE conditions required — (1) **measurable >10× performance benefit** for the subsystem's hot path, (2) **clear contract boundary** with TS callers (gRPC / HTTP / queue / FFI with stable schema), (3) **maintainer commitment to bilingual onboarding** for that subsystem (someone owns the cross-language story; we don't extract a Go subsystem if no maintainer is willing to keep its docs / examples / tests current). MVP-1 stays pure TS regardless. Decision authority for individual extractions: assistant proposes, user approves, ADR documents (per CLAUDE.md ADR rule) | Without a written rule, polyglot creep happens organically and produces the worst-of-all-worlds (multiple languages, no clear ownership, broken cross-language tooling). The 10× threshold is high on purpose — small perf wins don't justify the operational tax. Contract-boundary requirement preserves D-16 (subscribe-to-upstream) — kit consumers can swap a Go subsystem for a different impl as long as the contract holds. Bilingual-maintainer requirement is the real gate; without it, the language sprawls and skills don't transfer. Explicit veto: we never extract a subsystem just because someone prefers a language | — |

---

## How to add a new decision

The `harvest-knowledge` skill appends here automatically when sessions surface decisions. Manual additions follow the same row format:

- New ID = next free `D-NNN`
- Date = `YYYY-MM-DD` (ISO format)
- Area = short tag (`repo-location`, `license`, `git`, `tooling`, `workflow`, `scope`, `docs`, `architecture`, `auth`, `tenancy`, etc.)
- Decision = imperative one-liner
- Rationale = the *why*, especially the constraint or tradeoff that made this the right choice
- Link = ADR file if architectural-grade, otherwise `—`

**Rule:** if a decision is architectural (changes how subsystems interact, how data flows, what stack is used, what security model is enforced), it gets BOTH a row here AND a full ADR file. Otherwise this row alone is sufficient.

---

## Open / pending decisions (Phase B grooming)

These are flagged so future sessions know they're still open. They convert to D-12+ when grooming locks them.

| Area | Pending decision | Story |
|---|---|---|
| stack-frontend | Next.js / Remix / Astro / framework-agnostic component library | STORY-010 |
| package-manager | npm / pnpm / yarn (TS already locked via D-24) | STORY-010 |
| monorepo-tooling | Turborepo / Nx / pnpm workspaces (TS already locked via D-24) | STORY-010 |
| orm | Prisma / Drizzle / TypeORM / Kysely / native query | STORY-010 |
| ai-providers | Default LLM mix (Anthropic / OpenAI / Bedrock / Vertex / Ollama) for the LLM Gateway | STORY-010 |
| vector-db | pgvector / Pinecone / Weaviate / Qdrant / Chroma | STORY-010 |
| tenancy | Shared-DB / schema-per-tenant / DB-per-tenant | STORY-009 |
| event-bus | Postgres NOTIFY / Redis Streams / Kafka | STORY-009 |
| observability | OpenTelemetry + which backend default | STORY-009 |
| cloud-target | AWS / GCP / both | STORY-011 |
| iac-tool | Terraform / Pulumi / Crossplane / custom shell | STORY-011 |
| mvp-surface | Which ~6 subsystems are MVP-1 | STORY-012 |
| auth-provider | Self-hosted (Auth.js / Authelia / Ory) / third-party adapter (Auth0 / Clerk) | STORY-009 / STORY-010 |
| billing | Stripe-only / pluggable; MVP-1 or deferred | STORY-008 / STORY-012 |
| ml-platform | Pluggable / vendor-default / deferred to v2 | STORY-009 |
| data-quality | Per-table rules / lineage-aware / contract-based (Great Expectations / Soda) | STORY-009 |
