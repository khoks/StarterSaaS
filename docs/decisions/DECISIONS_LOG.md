# Decisions log

> Lighter-weight than ADRs. ADRs are full architectural decisions with context / alternatives / consequences. The decisions log is a chronological list of *every* meaningful decision — product, process, architectural, tooling — captured by the `harvest-knowledge` skill. ADR-grade decisions get cross-linked here AND get a full ADR file.
>
> Format: `D-NNN — date — area — title — one-line rationale — link (if any)`.

---

## Phase A bootstrap (this session)

| ID | Date | Area | Decision | Rationale | Link |
|---|---|---|---|---|---|
| D-1 | 2026-04-25 | repo-location | Project lives at `D:\DEV\ClaudeProjects\StarterSaaS` | Sibling to LearnPro; independent project; no shared monorepo | — |
| D-2 | 2026-04-25 | naming | Project name is `StarterSaaS` | User-confirmed; "happy-cocke" was a speech-to-text artifact and discarded | — |
| D-3 | 2026-04-25 | license | License is MIT | Maximum permissiveness for white-label adoption; adoption > protection | [ADR-0001](../architecture/ADR-0001-license-mit.md) |
| D-4 | 2026-04-25 | git | Default branch is `main` | GitHub default; will be PR-protected after Phase A | — |
| D-5 | 2026-04-25 | github | Remote is private repo `khoks/StarterSaaS` via `gh repo create` | Visibility flips to public when MVP-1 ships | — |
| D-6 | 2026-04-25 | tooling | Skill set: copy + adapt LearnPro's `harvest-knowledge` + `work-tracking`; add NEW `auto-pr` skill | Three-skill chain in Stop hook; preserves the proven prose+tracking flow and adds git automation | — |
| D-7 | 2026-04-25 | workflow | Auto-PR model: branch-per-session → push → `gh pr create` → `gh pr merge --auto --squash` | Keeps `main` always-green; PR queue is the audit trail | — |
| D-8 | 2026-04-25 | collaboration | Heavy upfront PM + engineer discussion on design / requirements / algorithms / tech stack BEFORE work; auto-merge after agreement | User explicitly requested. Auto-PR is a tool for landing already-agreed work, NOT a substitute for the discussion | — |
| D-9 | 2026-04-25 | scope | MVP-1 is a thin vertical slice end-to-end (~6 subsystems). Exact 6 chosen during Phase B/C grooming | Demonstrates every layer is reachable end-to-end, not that each subsystem exists in isolation | — |
| D-10 | 2026-04-25 | git | Bootstrap commit lands directly on `main` (no PR). After that, `auto-pr` takes over | Bootstrap *is* the PR-able infrastructure; can't PR before it exists | — |
| D-11 | 2026-04-25 | docs | Verbatim user vision goes into `docs/vision/RAW_VISION.md`. Transcription artifacts noted in a footnote, NOT edited inline | Body is source-of-truth for intent; editing inline would lose the user's framing | — |

---

## Phase B grooming

| ID | Date | Area | Decision | Rationale | Link |
|---|---|---|---|---|---|
| D-12 | 2026-04-27 | workflow | Phase B grooming runs as a single multi-session sweep through STORY-008 → STORY-012, with full PM treatment per open question (industry/competitive context + recommendation + tradeoffs + explicit invitation for user innovation + novelty analysis if user proposes one) | User explicitly chose this scope and cadence over piecemeal grooming or compact Q&A; instantiates D-8 for Phase B specifically; ensures the algorithmic-innovation probe `RAW_VISION.md` called for | — |
| D-13 | 2026-04-27 | persona | Primary persona is the **founder's first engineer** at a 1-5 person company. Secondary: solo founder (v1 "lite profile" — fewer layers enabled). Tertiary: small-team CTO inheriting a codebase where the first engineer already adopted the kit (lifecycle continuity) | This persona has authority + urgency + technical chops + felt-pain at once — the rare combination that produces deliberate, high-conversion adoption rather than churn. Whitespace in OSS-kit positioning: Supabase / Pocketbase / Appwrite / Firebase target founders generically, AWS / GCP target platform teams, nobody targets first engineers. Lifecycle continuity means today's first engineer is tomorrow's small-team CTO, so adoption happens earlier and grows with the company. Implies first-class migration-path support (engineer inherits existing code, not greenfield) — filed as a recommended addition | [GROOMED_FEATURES § Persona](../vision/GROOMED_FEATURES.md#persona) |
| D-14 | 2026-04-27 | workflow | Assistant is authorized to use `gh pr merge --admin --squash` to land **doc-only and tracking-only PRs** (housekeeping / grooming / decisions / ADRs) without waiting for a manual review approval. Branch protection on `main` stays strict (1 review required, markdown-lint required, linear history, no force-push). Source-code PRs (once Phase D begins) DO NOT get this override and must follow the normal review flow | User's solo-dev workflow: strict policy is right for code, but housekeeping PRs that only touch `docs/`, `project/`, `.claude/` would otherwise pile up indefinitely. Repo-level `allow_auto_merge` stays `false` — this is a deliberate per-PR escape hatch, not a blanket auto-merge. Scope discipline: the moment any PR touches `apps/`, `packages/`, `services/`, `infra/`, or `scripts/`, the override does not apply | — |
| D-15 | 2026-04-28 | differentiator + vision | StarterSaaS's headline differentiator (and core vision): **"AI-first, production-grade SaaS platform — 30+ integrated layers you will eventually need, white-labelable, one-command deployable."** Combines breadth (A) + production-grade (B) + one-command deploy (C) framings with a new dimension — **AI-first** — added by the user. Five named AI subsystems anchor the AI-first claim: (1) Event-driven Customer Profile Builder + AI-Native Stores, (2) Context-Resolving Query Service, (3) Agent Platform + Omnichannel Orchestrator with 6 registries, (4) LLM Gateway + Safety + Model Hub + Eval/Cost (foundational; MVP-1), (5) AI Coworker Platform for Internal Ops. CLAUDE.md mission updated; AI subsystems filed in NOVEL_IDEAS + RECOMMENDED_ADDITIONS | Whitespace in OSS positioning — no major OSS kit (Supabase / Pocketbase / Appwrite / Firebase / Strapi) is positioned AI-first. 2026 timing: every founder asks "how do I add AI?" and we answer before they ask. Persona fit: the founder's first engineer (D-13) is increasingly tasked with making the product AI-powered. Implies AI/ML platform moves from v2 (deferred) to MVP-1 candidate (foundational AI Subsystem 4 + subset of 3), expanding STORY-012 scope. Implies subscribe-to-upstream becomes more critical (Q3) because AI models/patterns evolve weekly | [GROOMED_FEATURES § Differentiator](../vision/GROOMED_FEATURES.md#differentiator) |
| D-16 | 2026-04-28 | kit-promise | Kit-promise model: **hybrid** — subscribe-to-upstream for the 30+ platform layers + 5 AI subsystems + adapter interfaces + deploy script; fork-once for white-label brand layer + product-specific business logic + custom UI screens. Mechanism: **packages + thin shell** — kit code ships as versioned packages, user repo is a thin shell importing them; adapter interfaces are package-exported contracts; user provides custom adapter implementations in shell. Concrete package manager TBD in STORY-010 (tech stack) | Persona D-13 (founder's first engineer) doesn't want to maintain platform code; subscribe-to-upstream matches their pain. AI-first D-15 means the 5 AI subsystems MUST be subscribable since models / prompts / safety techniques evolve weekly; pure fork-once would leave forks 4-5 model generations stale within 6 months. Pure subscribe-to-upstream removes the customization escape hatch for unusual product needs — the thin shell IS the escape hatch (vendor a package and customize when needed, accepting maintenance burden until you un-vendor). Pattern is well-validated in production by Saleor / Medusa / Drupal | [GROOMED_FEATURES § Kit-promise](../vision/GROOMED_FEATURES.md#kit-promise-model) |
| D-17 | 2026-04-28 | feature-scope | **AI-assisted upstream merge** confirmed as a first-class kit feature anchoring the AI-first + subscribe-to-upstream story. Mechanism: an agent detects upstream package updates → examines user customizations via adapter usage signatures → proposes a merge plan minimizing friction → tests the proposed merge against the user's test suite → surfaces conflicts to a human only when AI confidence is low. Implementation lives at the intersection of AI Subsystem 5 (Internal Ops Agent) and the kit-promise mechanism. Phase fit: **MVP-1** for the basic version (detect + propose + test); v1 for the full version (confidence scoring + autonomous-mode toggle). Implies AI Subsystem 5 needs a "lite" boundary — full Subsystem 5 is v2, but the merge-helper subset ships earlier | Combines two competitive moats — AI-first credibility + subscribe-to-upstream practicality — into one feature: "your kit upgrades itself." Direct response to "first engineer doesn't want to maintain platform code" pain (D-13) — even staying current is automated. No OSS kit ships "AI agent that helps you upgrade" as a first-class feature; component mechanisms (Dependabot / Renovate, Devin-style agents, LLM merge POCs) exist but the kit-aware-with-adapter-signatures + confidence-scored-escalation + integrated-as-first-class composition is novel. STORY-009 will design the ADR | [NOVEL_IDEAS — AI-assisted upstream merge](../vision/NOVEL_IDEAS.md) |
| D-18 | 2026-04-28 | site-topology | Pre-auth marketing site placement: **sibling repo** (`StarterSaaS-marketing` or per-adopter equivalent) as default; **monorepo with separate deploys** documented as a config-time alternative for users who prefer one-repo simplicity. Brand assets ship as a third package (`@starter-saas/brand`) by default, with opt-out for users who want manual duplication. Marketing site contains: hero / landing, pricing, blog, support documentation, contact, terms / privacy. Post-auth in-app help is separate (lives with the app). API / developer-portal docs deferred to STORY-009 — likely with the app for code-sync, but consumable via the marketing site | Persona D-13 (founder's first engineer) wants the app codebase clean. Marketing has fundamentally different deploy cadence, editor population, optimization target (SEO), and risk profile than the app — mixing them creates friction in every dimension. Pattern is industry-standard (Supabase / Vercel / Stripe / Pocketbase all separate). Sibling repo preserves D-15 (one-command deployable: deploy script `git clone`s both) and D-16 (each surface is independently subscribable). Monorepo alternative serves the secondary persona (solo founder) who prefers one-repo simplicity | [GROOMED_FEATURES § Site topology](../vision/GROOMED_FEATURES.md#site-topology) |
| D-19 | 2026-04-28 | site-topology | **Status page is separate** — third surface beyond app + marketing. Default: separate repo / managed service (Statuspage / Instatus / similar adapter). Lives at `status.<adopter-domain>`. Integrates with the kit's observability subsystem (incident management triggers status page updates via adapter) | Status page has its own deploy cadence, its own audience (customers + integrators), its own uptime story (must stay up when the app goes down — separate hosting is mandatory). Same logic as marketing-site separation but stricter on the uptime requirement. Adapter pattern preserves D-16 (subscribe-to-upstream) | [GROOMED_FEATURES § Site topology](../vision/GROOMED_FEATURES.md#site-topology) |
| D-20 | 2026-04-28 | white-label | White-label mechanism: **layered** — config-driven primary (90% of adopters), adapter overrides (8%), plugin extensions (2%). Each layer is the escape hatch for the previous. Config entry point: `starter.config.ts` (TypeScript with full type definitions for autocomplete + edit-time errors). Adapter pattern from D-16 supplies layer 2. Plugin extension points design is its own ADR (STORY-009). Code-gen explicitly rejected as a customization mechanism (used only for the initial scaffolding step `npx create-starter-saas`, which produces a minimal thin shell — not a customization vector) because code-gen breaks D-16 and D-17 | Layered approach matches industry-best (Strapi, Saleor, Next-auth all do this). Compatible with all locked decisions: D-13 fast-demo (edit config, deploy, done), D-16 subscribe-upstream (config is forward-compat), D-17 AI-assisted merge (config + adapter customizations have clear boundaries), D-18 brand-as-package (brand imported via config). Code-gen would have broken D-16 / D-17 — a hard "no" | [GROOMED_FEATURES § White-label mechanism](../vision/GROOMED_FEATURES.md#white-label-mechanism) |
| D-21 | 2026-04-28 | repo-topology | Kit development repo topology: **monorepo with workspaces**. One `khoks/StarterSaaS` repo containing `packages/<capability>/` workspaces (each publishing its own `@starter-saas/<capability>` package independently) + `apps/starter/` (the thin-shell template) + `tools/` (build / release / deploy scripts) + `docs/`. Each package has its own version, changelog, semver track. Final package-manager + monorepo-tooling pick (pnpm / yarn / npm; Turborepo / Nx / Bazel; etc.) deferred to STORY-010. **Publishing topology** stays polyrepo-shaped per D-16 — adopters install packages independently, no awareness of our internal monorepo | At our scale (1 active dev currently, modest contributor base), true polyrepo (one git repo per capability, ~40 repos) creates severe overhead: 40 CI configs, 40 branch-protection setups, 40x admin-override authorizations, coordinated cross-repo PRs for adapter contract changes, impossible atomic refactors, plugin discoverability problems, skills-replication burden. Industry-standard for OSS kits — Saleor, Medusa, Strapi, Vercel, Stripe internal, Shopify, Cloudflare workers SDKs, Next.js, Tanstack all ship monorepo-with-workspaces. Decision can be revisited if/when contributor count and capability ownership warrant per-repo extraction (one-way migration: easy to split later, near-impossible to un-split) | [GROOMED_FEATURES § Site topology](../vision/GROOMED_FEATURES.md#site-topology) |
| D-22 | 2026-04-28 | feature-scope | **AI-assisted config generation** confirmed as MVP-1 feature. First engineer describes the SaaS in natural language ("tutoring marketplace, need auth + payments + RBAC for teachers/students/admins, email notifications, basic analytics"); AI Subsystem 5 (Internal Ops Agent) generates a starting `starter.config.ts` + suggests adapter picks + scaffolds the thin-shell folder layout. Day-1 demo time drops from ~30 minutes to ~5. Lives at the intersection of AI Subsystem 5 (lite, MVP-1 subset per D-17 precedent) and the white-label config layer (D-20 layer 1). Trust-but-verify: engineer reviews + corrects AI output before applying — guardrail against hallucinated adapter picks | Pulls AI-first credibility into the very first user interaction with the kit. Pairs with D-13 (founder's first engineer must demo to founder fast) — compresses first hour to first minutes. Pattern of NL → config exists in fragments (Yeoman, create-* scripts, Vercel templates, AI dev assistants like Cursor / Devin) but no OSS kit ships kit-aware NL config generation that understands a fixed schema, suggests appropriate adapters, scaffolds folder layout, AND integrates with the white-label mechanism — that composition is novel | [NOVEL_IDEAS — AI-assisted config generation](../vision/NOVEL_IDEAS.md) |
| D-23 | 2026-04-28 | feature-scope | **AI-validated plugin compatibility** confirmed as MVP-1 feature. When a user writes a plugin against extension points, AI Subsystem 5 simulates upcoming kit upgrades against the plugin's hook signatures and surfaces likely breakage *before* the user merges an upstream update. Same agent vantage as D-17 (AI-assisted merge), different surface (D-17 looks at shell customizations; D-23 looks at plugins). Implies plugin extension-points must be machine-readable (good discipline anyway) | Removes the "plugin maintenance tax" that has historically choked plugin ecosystems (WordPress, Drupal). Encourages user-authored plugins (engineer can write a plugin without fearing silent break on upgrade). Combines with D-17 to make the entire subscribe-to-upstream story actually work, not theoretically work. Component mechanisms exist (TypeScript compat checking, semver enforcement, dependency analysis) but AI-driven plugin-vs-kit-version simulation as a first-class kit feature isn't documented in any OSS kit | [NOVEL_IDEAS — AI-validated plugin compatibility](../vision/NOVEL_IDEAS.md) |
| D-24 | 2026-05-02 | tech-stack | Backend stack: **TypeScript + Node.js + Fastify** on a **modular monolith** runtime model for MVP-1 (one process per service-shape, packaged as workspace packages within the monorepo per D-21). Node for BOTH production and local dev — Bun explicitly excluded (re-evaluate in v1+). MVP-1 ships pure TS — no Go / Rust / Python in the codebase. Polyglot escape hatch documented in D-26 for v1+ extraction of perf-critical subsystems with clear contracts. ADR-0002 to be written at STORY-010 closure | Persona D-13 (founder's first engineer): TS is the language they're most likely to know on day 1. AI-first D-15: TS has best-in-class AI SDK support (Vercel ai-sdk + Anthropic / OpenAI / AWS provider SDKs all first-class). Subscribe-to-upstream D-16: npm + pnpm is the most mature OSS package ecosystem. Monorepo D-21: Turborepo / Nx / pnpm workspaces are best-in-class. AI-validated plugin compat D-23: structural type system makes machine-readable extension-point specs natural. Full-stack consistency: same language as the frontend, lowering context-switch tax. Node over Bun: production maturity matters; Bun is promising but still maturing for prod-grade observability + stable hosting | — (ADR-0002 pending STORY-010 closure) |
| D-25 | 2026-05-02 | coding-standard | **TypeScript strict mode** (`"strict": true` in `tsconfig.json` for every package) AND **Zod schemas on every public boundary**: HTTP route inputs/outputs (Fastify-Zod plugin or equivalent), plugin extension points, event-bus message schemas, adapter interface contracts, `starter.config.ts` schema, AI agent input/output contracts. Internal-only code is allowed structural TS types without Zod. Shared between kit packages and adopter shell (the contract surface IS the boundary). ESLint + Prettier specifics deferred to STORY-010 closure / first PR landing source code | Strict mode + Zod everywhere is the coding-standards backbone of D-23 (AI-validated plugin compat) — an AI agent can only reason about extension points if those points have machine-readable shapes. Same logic for D-22 (AI-assisted config gen) — the config schema must be inspectable. Internal types are TS-only because Zod has runtime cost; only validate where data crosses a trust boundary (per existing CLAUDE.md "Validate at boundaries, trust internal code" rule, now made stack-specific). Pattern industry-validated by tRPC, Hono, Fastify-Zod, Effect Schema | — |
| D-26 | 2026-05-02 | polyglot-rule | **Polyglot escape-hatch rule** (governs when a subsystem earns extraction to Go / Rust / other in v1+): all THREE conditions required — (1) **measurable >10× performance benefit** for the subsystem's hot path, (2) **clear contract boundary** with TS callers (gRPC / HTTP / queue / FFI with stable schema), (3) **maintainer commitment to bilingual onboarding** for that subsystem (someone owns the cross-language story; we don't extract a Go subsystem if no maintainer is willing to keep its docs / examples / tests current). MVP-1 stays pure TS regardless. Decision authority for individual extractions: assistant proposes, user approves, ADR documents (per CLAUDE.md ADR rule) | Without a written rule, polyglot creep happens organically and produces the worst-of-all-worlds (multiple languages, no clear ownership, broken cross-language tooling). The 10× threshold is high on purpose — small perf wins don't justify the operational tax. Contract-boundary requirement preserves D-16 (subscribe-to-upstream) — kit consumers can swap a Go subsystem for a different impl as long as the contract holds. Bilingual-maintainer requirement is the real gate; without it, the language sprawls and skills don't transfer. Explicit veto: we never extract a subsystem just because someone prefers a language | — |
| D-27 | 2026-05-02 | tech-stack | Frontend frameworks: **Next.js (App Router)** for `apps/starter/` (the user's adopted-app template); **Astro** for `packages/marketing-template/` (the marketing site sibling-repo template per D-18); **framework-agnostic React** for `packages/ui/` (kit UI components, no Next-specific imports — adapters for Remix / Vite SPA / Astro islands land v1+). RSC enforced as default in `apps/starter/`; SPA mode is documented opt-in. ADR-0002 to capture both frontend + backend at STORY-010 closure | Next.js is the framework D-13 (founder's first engineer) is most likely to know; Vercel ai-sdk + Anthropic / OpenAI demo content is Next-first; RSC + streaming is best in class for AI-streaming UI (D-15 alignment). Astro for marketing serves D-18: best static / SEO / partial-hydration story, multi-framework support means non-engineer content team can use HTML / Vue / Svelte. Framework-agnostic UI components preserve D-16 — Next is the *default*, not the *lock*; adopters who want Remix or Vite SPA can still consume `packages/ui`. Single-framework-everywhere (alternative B) was rejected: loses Astro's SEO edge and forces non-engineers into React | — (ADR-0002 pending) |
| D-28 | 2026-05-02 | tech-stack | Frontend supporting libs: **Zustand** for client-side state (RSC handles server-state without a client lib like TanStack Query in MVP-1), **shadcn-ui pattern** for component primitives (Radix UI primitives + copy-paste-customizable styles, themed via `@starter-saas/brand`), **Tailwind** (v4 if mature; v3 fallback) for CSS, **react-hook-form + Zod** for forms (consistent with D-25 — Zod everywhere). ESLint + Prettier specifics finalized at first-PR-with-source-code (Phase D) | All picks are industry default for the "first engineer at a startup" persona. shadcn-ui pattern keeps adopters in control of code (no version-bump-breaks-UI risk; aligns with D-16 escape-hatch ergonomics). Tailwind chosen for AI-tool friendliness (Cursor / Copilot generate it well) and theming via brand package. react-hook-form + Zod is the de-facto stack and feeds D-25 boundary-validation discipline | — |
| D-29 | 2026-05-02 | feature-scope | **`packages/ai-ui`** is an MVP-1 deliverable: prebuilt React components for streaming-message rendering, agent-step visualization, token-counter, RAG-source-citation, prompt-input-with-attach, and tool-call-card. Framework-agnostic React (D-27); usable in Next App Router, Remix, Astro islands, Vite SPA. Anchors the AI-first UI claim (D-15) — first user-facing AI feature ships with the kit, not as an afterthought | The AI-first differentiator (D-15) is hollow if the kit doesn't ship visible AI primitives. Component mechanisms exist (Vercel ai-sdk + custom UI; LangChain.js streaming demos) but no OSS kit ships a *cohesive AI UI primitives package* themed by a brand layer + integrated with the kit's LLM Gateway adapter. ADR in STORY-009 covers: streaming protocol contract, component-to-AI-Subsystem-3-orchestrator integration, accessibility patterns for streaming text | — |
| D-30 | 2026-05-02 | scope | **i18n deferred to v1+**. MVP-1 ships English only. v1 lands an i18n adapter (likely `next-intl` for Next App Router + `astro-i18n` for marketing-template), with `@starter-saas/i18n` package coordinating the brand + content + UI translations | Keeps MVP-1 scope tight; i18n is a non-trivial cross-cutting concern (RTL languages, plural rules, ICU MessageFormat, content management workflows) that benefits from being designed once after observing real adopter usage. Persona D-13 (US-centric founder's first engineer) typically ships English-first SaaS. Defer matches a known good pattern (Vercel / Next-auth / Stripe all started English-first) | — |
| D-31 | 2026-05-02 | tech-stack | Package manager + monorepo tooling: **npm + npm workspaces + Turborepo**. npm chosen over pnpm for **zero-install ergonomics** — every contributor's Node already has it; no `npm i -g pnpm` step. Turborepo orchestrates tasks + caching across the workspace tree (Turborepo officially supports npm workspaces). Trade: looser dependency discipline than pnpm; we'll lean harder on D-25 (TS strict + Zod) and CI guards to catch phantom-dep issues — revisit pnpm at v1+ if phantom-dep issues materialize | npm's universal availability is decisive for D-13 (founder's first engineer): zero-friction onboarding for kit contributors AND for adopters' shells. pnpm's strict semantics were tempting but the onboarding tax outweighs the benefit at current scale | — (ADR-0002 pending) |
| D-32 | 2026-05-02 | tech-stack | Data layer: **Drizzle ORM + drizzle-kit migrations + PostgreSQL primary engine**. Connection pooling: **PgBouncer adapter for production, native node-postgres pool for dev**. Adapters for MySQL / SQLite slated v1+ if user demand exists | Drizzle's TS-first schema-as-data property compounds with D-15 (AI-first) and D-23 (AI-validated plugin compat) — agents read plain TS schema files, not a custom DSL. pgvector support is native first-class (D-15 needs vector ops). Lightweight runtime preserves D-26 polyglot-rule optionality (no Rust engine adding hidden coupling). PostgreSQL: best multi-tenant (D-33) + pgvector + JSONB story for the kit's stack. PgBouncer is the production-grade pooling default; native pool in dev keeps the local loop fast. Trade-off: Prisma has better mainstream DX but its custom DSL is less AI-introspectable and its Rust engine adds runtime weight — Drizzle wins for our specific kit | — (ADR-0002 pending) |
| D-33 | 2026-05-02 | tenancy | Multi-tenancy isolation model: **schema-per-tenant** in PostgreSQL. Each adopter's tenants get their own Postgres schema; kit-supplied tables (auth / RBAC / etc.) get instantiated per schema. Cross-tenant queries (admin / billing / observability aggregation) explicitly use schema-qualified queries through dedicated "platform" schemas. **Pre-locked here at user direction**; full architectural ADR-0004 (multi-tenancy) drafted in STORY-009 referencing this row | Schema-per-tenant gives strong isolation (security audits cleaner, accidental cross-tenant data leaks much harder than shared-DB-with-tenant-id), supports per-tenant performance tuning (different connection limits, separate vacuums), and fits PostgreSQL native operations well. Trade-offs: harder migrations (must run against N schemas), connection-pool count grows with tenant count (mitigated by PgBouncer per D-32), schema-creation is itself a per-onboarding action. DB-per-tenant rejected (too heavy operationally for D-13 persona). Shared-DB-with-tenant-id rejected by user for security/ergonomics. ADR-0004 will design the per-schema migration runner + cross-schema query primitives + tenant-provisioning flow | (ADR-0004 pending in STORY-009) |
| D-34 | 2026-05-02 | ai-stack | LLM provider adapters in MVP-1: **Anthropic + OpenAI + Ollama**. Anthropic for the kit's own AI-assistance features (per D-35) + adopter use; OpenAI for adopter use (most engineers already familiar); Ollama for local-dev / on-prem / regulated-industry deployments. v1+ adapter slate: AWS Bedrock (Claude/Llama/Titan), Google Vertex (Gemini), Azure OpenAI, Together / Replicate / OpenRouter (open-model hosts) | Persona D-13 (founder's first engineer) reaches for Anthropic or OpenAI by default; both are mandatory. Ollama covers the on-prem / privacy-conscious adopter narrative without which the AI-first claim feels vendor-locked. The 3-adapter MVP-1 set covers proprietary-frontier + alternative-proprietary-frontier + local — industry-standard breadth | (ADR-0002 pending) |
| D-35 | 2026-05-02 | ai-stack | Kit-default model for AI-assistance features (D-17 AI-assisted upstream merge, D-22 AI-assisted config generation, D-23 AI-validated plugin compatibility): **Claude Opus 4.7**. User overrode the assistant's Sonnet 4.6 recommendation in favor of higher reasoning quality at higher cost (~5× per call), partially offset by D-38 prompt caching on repeated kit context. Adopter overrides via `starter.config.ts` to use Sonnet, Haiku, GPT-4o, or any other configured provider | AI-first positioning (D-15) is hollow if the kit's own AI features feel mediocre; user explicitly traded cost for quality. Opus 4.7 is genuinely the strongest reasoning model for code-aware tasks (merge conflict semantics, plugin extension-point analysis) at this writing. The cost premium is acceptable because: (a) D-38 prompt caching reduces marginal cost for repeated kit context, (b) AI-assistance features run at engineer-action frequency not user-traffic frequency, (c) AI-first credibility is a one-shot first-impression bet | (ADR-0002 pending) |
| D-36 | 2026-05-02 | ai-stack | Embedding model adapters in MVP-1: **OpenAI text-embedding-3-small as default + Voyage AI adapter + Ollama-hosted-model adapter**. Used by D-15 Sub 1 (Customer Profile Builder + AI-Native Stores) and D-15 Sub 2 (Context-Resolving Query Service) for vector ops against pgvector (D-37) | OpenAI text-embedding-3-small is the most widely-supported, cost-effective, reasonable-quality default. Voyage AI is Anthropic-recommended for retrieval and serves the OpenAI-averse adopter. Ollama-hosted (BGE / E5 / nomic-embed-text) covers the on-prem / privacy-conscious narrative consistent with D-34. Embedding-model migration is non-trivial (vectors regenerate when model changes) — adopter picks once and commits; v1 documents the migration path | (ADR-0002 pending) |
| D-37 | 2026-05-02 | ai-stack | Vector DB primary engine: **pgvector** (lives in the same PostgreSQL instance as D-32 — zero additional deploy). v1+ adapter slate: Qdrant (open-source, Rust, performant), Pinecone (managed cloud), Weaviate (open-source, feature-rich) | Zero-additional-deploy is decisive at MVP-1 scale (founder's first engineer demo flow stays simple). D-33 schema-per-tenant works natively (each tenant's vectors live in their own schema). Industry trend has pgvector winning for AI-app-scale workloads (Supabase recommends; many production AI apps now use pgvector instead of Pinecone). HNSW indexes give acceptable performance through significant scale; adopter swaps adapter when they outgrow it. Reduces D-15 / D-16 / D-32 / D-33 coupling — one storage engine, one tenancy story, one connection pool | (ADR-0002 pending) |
| D-38 | 2026-05-02 | ai-stack | AI observability + cost management ships **MVP-1**, not deferred: (1) **Cost dashboards** — per-provider / per-tenant / per-feature LLM spend visibility (already required by D-15 AI Sub 4); (2) **Per-tenant LLM budget** with hard / soft limits — without it the AI-first kit risks unbounded adopter cost; (3) **Anthropic prompt caching** — leveraged transparently when the active provider supports it, fallback when not (provides D-35 Opus 4.7 cost relief on repeated kit context); (4) **Adopter-config-driven routing** — `starter.config.ts` declares fallback chains (e.g., "prefer Haiku, fallback to GPT-4o-mini if rate-limited"); kit ships sensible defaults | AI-first means cost-aware-by-default. RECOMMENDED_ADDITIONS' "AI cost management + per-tenant LLM budgeting" entry elevated from "proposed" to "in scope MVP-1" by this decision. Without these guardrails, the kit's first production deploy could surprise an adopter with a five-figure bill — that's a brand-killing failure mode for a kit the first engineer recommended to their founder | (ADR-0002 pending) |
| D-39 | 2026-05-02 | cloud-target | Cloud target: **both AWS and GCP supported from MVP-1.** Single deploy targets one cloud per adopter (multi-cloud-per-adopter is out of MVP-1 scope; v1+ for cross-region / cross-cloud). Each cloud gets its own kit packages (`@starter-saas/infra-aws`, `@starter-saas/infra-gcp`) with independently-versioned IaC modules. `starter.config.ts` declares `cloud: "aws" \| "gcp"`; deploy script (Q2) picks modules accordingly | D-15 headline pitch literally says "one-command deployable to AWS or GCP" — it's load-bearing, not aspirational. Single-cloud MVP-1 would lose ~50% of D-13 (founder's first engineer) audiences at first contact, since adopters arrive already on one cloud or the other. The 2× scope tax on infra is real but <2× in practice (shared adapter contracts; per-cloud impls behind unified interfaces per D-16). ADR-0003 will document the cloud-resource adapter contract spec | (ADR-0003 pending in this story) |
| D-40 | 2026-05-02 | iac-tool | Infrastructure-as-Code tool: **Pulumi (TypeScript)**. AWS + GCP providers are mature. Pulumi programs ship as part of the kit's monorepo (`packages/infra-aws`, `packages/infra-gcp`, `packages/infra-shared`). Adopter's `starter.config.ts` is consumed by Pulumi at deploy time | Pulumi as TS code matches D-24 (single-language stack — engineer doesn't context-switch to HCL). AI-introspectable for D-22 (config gen), D-23 (plugin compat), D-17 (upstream merge — IaC drift detection is a future ADR extension). Adapter pattern (D-16) wraps cleanly: each cloud's resources are TS modules adopters can subscribe-to-upstream. Trade-off: smaller community than Terraform; mitigation — Pulumi's AWS + GCP providers are mature and well-maintained. CDK-TF (Terraform via TS layer) was the credible alternative; rejected because Pulumi is purpose-built for TS-native IaC, not retrofitted | (ADR-0003 pending) |
| D-41 | 2026-05-02 | cloud-arch | Cloud-resource architecture (MVP-1): (a) **Module structure**: `packages/infra-shared/` (cloud-agnostic contracts + types); `packages/infra-aws/` (RDS Aurora Postgres + pgvector extension + Secrets Manager + ECS Fargate Ollama + ECS / Lambda LLM Gateway + CloudWatch + OTel collector + VPC); `packages/infra-gcp/` (Cloud SQL Postgres + pgvector + Secret Manager + Cloud Run / GKE Ollama + Cloud Run LLM Gateway + Cloud Logging + OTel collector + VPC). (b) **Default regions**: `us-west-2` for AWS, `us-west1` for GCP (geographically aligned — both Oregon). Default is a *prompt suggestion*, not a silent fallback — deploy script asks adopter to confirm or override. (c) **Postgres**: managed (RDS Aurora / Cloud SQL) MVP-1; self-hosted-in-VPC adapter v1+ for cost-conscious / regulated adopters. (d) **Ollama**: self-hosted in VPC pod, opt-in (no managed Ollama service exists). (e) **One-cloud-per-deploy** at MVP-1; multi-region / multi-cloud per adopter is v1+ | Module structure preserves D-16 (each cloud package is independently subscribable). us-west-2 / us-west1 alignment lets adopters span clouds with consistent latency profile if they later go multi-cloud (v1+). Prompted-default-region matches D-22 spirit — kit suggests sensible defaults but adopter explicitly confirms (no silent geographic surprises). Managed Postgres serves D-13 ergonomics; self-hosted v1+ adapter serves cost-sensitive cases without bloating MVP-1. Self-hosted Ollama is the only option since no major cloud offers managed Ollama — VPC pod isolation keeps the privacy story intact | (ADR-0003 pending) |
| D-42 | 2026-05-02 | deploy-cli | Deploy CLI architecture: **`@starter-saas/cli` TS CLI tool** with subcommands `init` / `deploy` / `tenant` / `teardown` / `doctor`. **10-step idempotent deploy flow**: (1) validate `starter.config.ts` via Zod, (2) cloud auth check, (3) state-backend bootstrap (S3 / GCS Pulumi state bucket), (4) AI-assisted config offer (opt-in via `--ai-assist`), (5) network provisioning, (6) data layer + secrets, (7) schema-per-tenant init via drizzle-kit, (8) app services + LLM Gateway + optional Ollama, (9) smoke tests, (10) success summary. **Pulumi-native idempotency** (re-runnable; failed deploys leave state in Pulumi backend so re-run resumes). **Interactive secrets default** + `--env-file` + `--from-secrets-manager` for non-interactive paths. **Anonymous telemetry opt-in default-OFF** (privacy-first; never default-on). **Triple-confirm teardown** with explicit data-loss warning. **Default 30-min deploy timeout** as safety net (NOT primary visibility — D-43 portal handles that). `doctor` subcommand MVP-1; `--dry-run` flag deferred to v1+ | TS CLI matches D-24 stack consistency; cross-OS-portable (Mac/Linux/Windows-with-Node); subcommand-extensible. 10-step flow documents the implicit ordering; each step is idempotent so partial failures don't strand state. Interactive secrets serve D-13 first-deploy demo; `--env-file` serves CI. Telemetry default-off respects D-13 privacy concerns. Triple-confirm teardown prevents fat-finger destruction. Default timeout is a safety net, not the visibility primary — D-43 portal is. `doctor` MVP-1 because adopters need self-service drift diagnosis from day 1 | (ADR-0003 pending) |
| D-43 | 2026-05-02 | deploy-ux | **Deploy command portal — MVP-1 feature.** When `@starter-saas/cli deploy` runs, it spins up a **local web server (default port 7732 with conflict detection)** that opens a browser-based dashboard (TUI fallback for headless / `--no-open`). Portal shows: (a) real-time per-step status across the 10-step flow (D-42), (b) **pause / resume / abort** controls, (c) streamed step logs, (d) **AI-generated narration** per step ("provisioning RDS Aurora cluster — typically takes 8-12 minutes; this step does X, Y, Z") via the LLM Gateway (D-15 Sub 4) with aggressive prompt caching (D-38). Replaces "trust the CLI logs and the deploy timeout" UX with full visibility + control. User-proposed in Q2 of STORY-011 | Adopter pain: long-running deploy with terminal-only progress = "is it stuck?" anxiety; default 30-min timeout from D-42 is a safety net but not a UX. D-13 (founder's first engineer) demo flow benefits massively from a clean visible dashboard rather than a scrolling terminal. Pause/resume/abort prevents fat-finger out-of-band kills (Ctrl-C in the terminal can leave Pulumi state half-applied — portal abort routes through proper cleanup). AI narration explains arcane steps to non-experts. Local server means no cloud dependency for the UI itself. **Filed in NOVEL_IDEAS** — medium novelty: closest analogues are Vercel/Heroku hosted dashboards (post-deploy) and Pulumi Cloud (hosted); local-while-CLI-runs with pause/resume + AI narration as a first-class kit feature isn't documented in any OSS kit | [NOVEL_IDEAS — Local command portal](../vision/NOVEL_IDEAS.md) |
| D-44 | 2026-05-02 | tenancy-detail | Multi-tenancy detail (extends D-33 schema-per-tenant). **Migration runner**: `@starter-saas/cli tenant migrate` subcommand iterates `platform.tenants`, runs forward-only migrations against each tenant schema, tracks state in `platform.tenant_migrations`, default 5 parallel migrations (configurable), continue-on-error default with `--fail-fast` flag, `--dry-run` shows planned migrations. **Cross-schema query primitives**: (a) `platform` schemas hold cross-tenant data (directory, billing, audit) accessed via schema-qualified queries; (b) TS-side `withTenants()` wrapper for ad-hoc admin iteration; (c) federated views explicitly rejected (brittle as schemas evolve). **Connection pool isolation**: PgBouncer transaction-mode + per-tenant rate-limit middleware in Fastify (default 100 queries/sec/tenant, configurable); no per-tenant DB pools. **Tenant provisioning saga (9 steps)**: reserve ID → CREATE SCHEMA → run migrations → seed defaults → provision secrets → register billing → mark active → emit `tenant.provisioned` event → send welcome. Idempotent (resume from last incomplete), compensating (per-step rollback), observable (every step publishes events to ADR-0005 bus). **Schema deletion / archival**: 2-stage — soft archive (immediate; `archived_at` set; reads return 410 Gone) → hard delete after 30-day retention default (configurable; `DROP SCHEMA CASCADE`); audit row in `platform.tenant_archive_log`. **Legal hold**: `legal_hold` boolean per tenant blocks hard delete until cleared (GDPR vs. retention reconciliation; surfaced in `doctor`). **Restore**: during soft-archive window, `tenant restore <id>` flips `archived_at` back. **Side picks**: tenant ID = UUIDv7; schema name = `tenant_{uuid}` literal; `platform` schemas co-resident in same Postgres DB; per-tenant query budget default 100 q/s; soft-archive retention default 30 days; migration parallelism cap default 5 (all adopter-tunable in `starter.config.ts`) | Pattern stack avoids the worst-of-each-world: platform-schemas are honest about cross-tenant data; `withTenants()` wrapper handles ad-hoc admin without needing federated-view brittleness; PgBouncer transaction mode + app-side rate limit prevents one tenant starving others without exploding pool count. 9-step saga is the kit's **first concrete saga** — its requirements (ordered events, at-least-once, idempotency keys, DLQ) drive ADR-0005 (event bus) design in Q2. 2-stage archival reconciles GDPR right-to-erasure with audit retention via `legal_hold` flag. Continue-on-error migration policy chosen because schema-per-tenant means N independent failure surfaces — one tenant's bad migration shouldn't block 99 others. UUIDv7 chosen for sortability + opacity (k-sortable benefit for indexes; opaque for security) | [ADR-0004](../architecture/ADR-0004-multi-tenancy.md) |
| D-45 | 2026-05-05 | event-bus + saga | Event bus + saga choreography pattern. **Bus contract surface = Kafka-shaped from day 1**: events have explicit `topic` + `partition_key` + `consumer_group` + `idempotency_key` + headers (correlation_id / trace_id / saga_id). Two adapters in scope: **MVP-1 default `@starter-saas/event-bus-pg-outbox`** (Kafka semantics implemented over Postgres NOTIFY + outbox table — atomic-with-business-state, durable, ordered-per-partition-key, at-least-once) and **v1 target `@starter-saas/event-bus-kafka`** (native Kafka — drop-in adapter swap via `starter.config.ts`; adopter code unchanged). Additional v1+ adapters: Redis Streams, AWS EventBridge, GCP Pub/Sub. Kafka is the **target state** — Postgres outbox is the MVP-1 stand-in, NOT a separate paradigm. **Outbox pattern**: producer writes event to `platform.outbox` in same DB transaction as business operation (no dual-write); background poller (default 100ms interval, configurable) emits NOTIFY events; consumers LISTEN per topic and dedupe via `platform.event_dedupe` keyed by `(consumer_group, idempotency_key)` with default 30-day TTL. **Saga choreography pattern**: event-driven (no central orchestrator), saga state machines encoded in TS code, saga instances tracked in `platform.saga_instances` (state + per-step status + payload), each step subscribes to previous step's completion event + business prerequisites, compensating actions registered alongside each step (failure → walk compensation registry in reverse order; compensations themselves emit events for observability). **Saga primitives separation**: `packages/event-bus` (bus core + adapter contracts + pg-outbox impl) and `packages/saga` (state machine + compensation registry + instance tracking). **Side picks**: outbox poll interval 100ms; max retry count 5 (then DLQ at `platform.event_dlq`); saga instance retention 90 days post-completion; idempotency key required field with pattern `{producer_name}:{business_id}:{action}`; saga step timeout default 30s; partition_key = saga_id for saga events (preserves ordering); NOTIFY channel naming `tenant_{uuid}` for tenant-scoped + `platform` for cross-tenant (all adopter-tunable) | User-driven design discipline: Kafka is the target end-state (v1), so MVP-1 must architect to migrate cleanly — no contract-breaking changes when the kafka adapter ships. The pg-outbox adapter implements Kafka semantics over Postgres, not a Postgres-NOTIFY-flavored alternative. Same-transaction outbox writes solve the dual-write problem (atomic with business state). Choreography over orchestration matches D-13 persona (no Temporal / Conductor operational dependency); revisit if sagas grow >15 steps. Separate `packages/saga` because saga primitives are useful even outside an event bus (internal step machines, in-process choreography). UUIDv7-style partition keys preserve sortability + opacity for traceability. Industry-validated pattern (Microservices.io / Confluent / pg-boss / Inngest all use outbox; Kafka contract surface is the de-facto standard for event streaming) | [ADR-0005](../architecture/ADR-0005-event-bus.md) |
| D-46 | 2026-05-05 | observability | Observability stack. **Foundation**: OpenTelemetry (OTel) SDK with OTLP wire protocol; vendor-agnostic backend; adopts OTel **GenAI semantic conventions** (`gen_ai.system`, `gen_ai.request.model`, `gen_ai.usage.{input,output,cache_read}_tokens`, etc.) from day 1. **Logs**: Pino → OTel Logs Bridge. **Metrics**: OTel SDK + Prometheus-compatible export. **Traces**: OTel SDK + auto-instrumentation for Fastify + Drizzle + Pulumi. **Backends MVP-1**: per-cloud default — AWS CloudWatch + X-Ray (when `cloud: aws`); GCP Cloud Logging + Cloud Trace + Cloud Monitoring (when `cloud: gcp`); **plus Langfuse** for LLM-specific deep-dive (agent flows / LangGraph node call tracing / prompt-response capture / eval — runs alongside OTel, not as a replacement). **Backends v1+**: self-hosted Grafana stack (Grafana + Loki + Tempo + Mimir), Datadog, New Relic, Honeycomb. **Cost dashboard MVP-1**: per-provider / per-tenant / per-feature spend + budget consumed % + cache hit rate + token-distribution histogram, themed React admin dashboard at `/admin/observability`. **Per-tenant LLM budget enforcement MVP-1** (extends D-38): `platform.tenant_budgets` table with hard / soft limits + 80% soft-threshold default + adopter-configurable defaults in `starter.config.ts`. **Side picks**: adaptive trace sampling (100% on errors, 1% on healthy paths, adopter-tunable); PII scrubbing required at MVP-1 (Pino redactor + OTel attribute redactor; LLM prompt content NOT logged in full by default — token counts + first/last 100 chars + content hash for dedupe); retention defaults logs 30d / traces 7d / metrics 90d / cost-rollups 90d hot + archive (all adopter-tunable); kit ships sensible default alerts (deploy failure / saga DLQ growth / budget breach / error spike) → adopter's existing alerting via webhook; dashboards as TS code in `packages/observability-dashboards` | OTel as foundation = vendor-agnostic + future-proof + rich GenAI semconv. Cloud-native default MVP-1 honors D-13 zero-additional-deploy story (uses D-41 cloud resources). Langfuse adoption MVP-1 acknowledges that OTel GenAI semconv is generic — Langfuse is purpose-built for LLM-app observability (agent decision trees, prompt-response inspection, eval framework). Two-headed observability for LLM calls: OTel for general traces + Langfuse for LLM-specific deep-dive; cost doubled per call mitigated by adaptive sampling. PII scrubbing-by-default protects D-13 persona's GDPR / privacy stance. Industry-validated: every modern LLM-app stack converges on OTel-or-Langfuse + cloud-native or Grafana for general telemetry | [ADR-0006](../architecture/ADR-0006-observability.md) |
| D-47 | 2026-05-05 | llm-gateway | LLM Gateway architecture (D-15 Sub 4 detail). **Gateway is a policy enforcement point + observability choke point — NOT a routing brain.** Per user direction: **provider / model selection happens at the capability / component level, NOT centralized in the Gateway.** Each kit feature / agent / capability declares its preferred provider + model in its own config (e.g., AI-assistance features default to Opus 4.7 per D-35; embedding-generating features pick text-embedding-3-small per D-36; conversational agents pick per-skill); the Gateway just intercepts the call and applies cross-cutting policy. **Gateway responsibilities** (the 4 components from D-15 Sub 4): (1) **Gateway core** — request interception + observability span emission (OTel GenAI semconv per D-46) + Langfuse trace emission + cost-event publish to event bus per D-45; (2) **Safety layer** — PII / PCI scrubbing on prompts AND responses; prompt injection detection; profanity / illegal-advice / toxicity / bias filtering; output schema validation when caller declares one; configurable per-tenant via `starter.config.ts`; (3) **Model Hub** — registry of available providers / models (Anthropic / OpenAI / Ollama MVP-1 per D-34); supports adopter-hosted custom models (e.g., fine-tuned LLMs in their VPC); surfaces "latest models available" data + provisioned-throughput-request workflow (v1+); (4) **Eval / Cost** — eval framework hooks (capture call → run eval suite → store results in `platform.eval_results`); cost tracking (per-call + rolled up by event-bus consumer per D-46); per-tenant budget enforcement with hard / soft limits per D-46 + D-38. **Adopter-config example**: each feature declares `{ provider, model, fallback?, eval_suite? }`; adopter overrides via `starter.config.ts`. **Refines D-35 + D-38**: D-35 (Opus 4.7 kit-default for AI-assistance) is the FEATURE-level default for D-17/D-22/D-23, not a Gateway-level default. D-38's "adopter-config-driven routing" is now per-feature config, not a Gateway global rule | User-driven refinement: routing logic at the Gateway is over-centralization that fights real-world feature diversity. Each capability has different cost / quality / latency requirements (an embedding call wants the cheapest fastest model; a code-merge agent wants the most capable; a classification call wants the most consistent). Per-component selection lets each feature optimize for its needs while the Gateway provides cross-cutting governance. Gateway-as-policy-point is industry-standard pattern (Cloudflare Workers AI Gateway, AWS Bedrock guardrails, OpenAI Moderation API all sit at the call-site as policy, not as routers). PII / PCI / safety enforcement at the Gateway means adopters get correct-by-default behavior without per-feature integration burden. Eval framework integration MVP-1 (with results persisted) gives the kit's AI features measurable improvement over time — feeds D-23 AI-validated plugin compatibility | [ADR-0011](../architecture/ADR-0011-llm-gateway.md) |

---

## How to add a new decision

The `harvest-knowledge` skill appends here automatically when sessions surface decisions. Manual additions follow the same row format:

- New ID = next free `D-NNN`
- Date = `YYYY-MM-DD` (ISO format)
- Area = short tag (`repo-location`, `license`, `git`, `tooling`, `workflow`, `scope`, `docs`, `architecture`, `auth`, `tenancy`, etc.)
- Decision = imperative one-liner
- Rationale = the *why*, especially the constraint or tradeoff that made this the right choice
- Link = ADR file if architectural-grade, otherwise `—`

**Rule:** if a decision is architectural (changes how subsystems interact, how data flows, what stack is used, what security model is enforced), it gets BOTH a row here AND a full ADR file. Otherwise this row alone is sufficient.

---

## Open / pending decisions (Phase B grooming)

These are flagged so future sessions know they're still open. They convert to D-12+ when grooming locks them.

| Area | Pending decision | Story |
|---|---|---|
| event-bus | Postgres NOTIFY / Redis Streams / Kafka | STORY-009 |
| observability | OpenTelemetry + which backend default | STORY-009 |
| mvp-surface | Which ~6 subsystems are MVP-1 | STORY-012 |
| auth-provider | Self-hosted (Auth.js / Authelia / Ory) / third-party adapter (Auth0 / Clerk) | STORY-009 / STORY-010 |
| billing | Stripe-only / pluggable; MVP-1 or deferred | STORY-008 / STORY-012 |
| ml-platform | Pluggable / vendor-default / deferred to v2 | STORY-009 |
| data-quality | Per-table rules / lineage-aware / contract-based (Great Expectations / Soda) | STORY-009 |
