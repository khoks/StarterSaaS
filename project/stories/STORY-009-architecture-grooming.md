---
id: STORY-009
title: Architecture grooming — multi-tenancy, event bus, observability
type: story
status: in-progress
priority: P0
estimate: XL
parent: EPIC-002
phase: scaffolding
tags: [grooming, architecture, phase-b]
created: 2026-04-25
updated: 2026-05-02
---

## Description

As an engineer (the user's eng hat), I need the cross-cutting architecture decisions locked: multi-tenancy isolation (shared-DB / schema-per-tenant / DB-per-tenant), event bus (Postgres NOTIFY / Redis Streams / Kafka), saga choreography pattern, observability stack (OpenTelemetry default + which backend), data-quality framework, and plugin / extension architecture. Each lands as an ADR.

## Acceptance criteria

### Original ACs (from STORY-009 spec)

- [x] **ADR-0004** multi-tenancy detail accepted (per-schema migration runner + cross-schema query primitives + tenant-provisioning flow; extends D-33) — *locked 2026-05-02 as D-44; ADR-0004 written and accepted*
- [x] **ADR-0005** event bus selection accepted (Postgres NOTIFY / Redis Streams / Kafka) — *locked 2026-05-05 as D-45; Kafka-shaped contract surface from day 1; MVP-1 = pg-outbox adapter; v1 target = native Kafka adapter*
- [x] **ADR-0006** observability stack accepted (OTel + backend default + LLM-trace extension; extends D-38) — *locked 2026-05-05 as D-46; OTel + GenAI semconv + cloud-native MVP-1 + Langfuse MVP-1 + Grafana/Datadog/Honeycomb v1+*
- [x] Saga choreography pattern documented in `docs/architecture/ARCHITECTURE.md` — *locked 2026-05-05 as part of D-45 / ADR-0005: event-driven choreography (no central orchestrator); TS-coded state machines; `packages/saga` for primitives; reference impl = ADR-0004's 9-step provisioning saga*
- [ ] Plugin / extension architecture sketched (extends D-20 layered)
- [ ] Data-quality approach (per-table rules / lineage-aware / contract-based) decided and documented

### Absorbed from STORY-008 / STORY-010 / STORY-011 grooming (queued ADRs)

- [x] **ADR-0007** auth provider strategy (self-hosted Auth.js / Authelia / Ory vs. third-party adapter Auth0 / Clerk) — *locked 2026-05-05 as D-48; Auth.js v5+ MVP-1 default + Drizzle adapter + tenant-aware via `platform.user_tenant` + RBAC sketch + Clerk/Auth0/Cognito/Vertex/WorkOS/Authelia/Ory v1+ adapters*
- [x] **ADR-0008** AI Subsystem 1 — Event-driven Customer Profile Builder + AI-Native Stores (D-15 Sub 1) — *locked 2026-05-05 as D-50; foundation MVP-1 + subsystem v1; 5 default profile types; pgvector storage; streaming incremental updates*
- [x] **ADR-0009** AI Subsystem 2 — Context-Resolving Query Service (D-15 Sub 2) — *locked 2026-05-05 as D-51; v1; 3-layer pipeline (plan → execute → synthesize); permission filter at execute layer*
- [x] **ADR-0010** AI Subsystem 3 — Agent Platform + Omnichannel Orchestrator with 6 registries (D-15 Sub 3) — *locked 2026-05-05 as D-52; MVP-1 web subset + LangGraph orchestrator + 4 of 6 registries; v1 ships subagents + mobile + non-UI embed; v1+ telephonic*
- [x] **ADR-0011** AI Subsystem 4 — LLM Gateway + Safety + Model Hub + Eval/Cost (D-15 Sub 4; foundational MVP-1) — *locked 2026-05-05 as D-47; Gateway = policy enforcement point + observability choke point (NOT routing brain); provider/model selection per-component; refines D-35 + D-38*
- [x] **ADR-0012** AI Subsystem 5 — AI Coworker Platform for Internal Ops (D-15 Sub 5; v2 with merge-helper subset MVP-1) — *locked 2026-05-05 as D-53; MVP-1 lite via D-17/D-22/D-23; v2 full conversational + workflows + scheduled tasks; architecture registry foundation MVP-1*
- [ ] **ADR-0013** Plugin extension-point machine-readable spec + AI-validated compat methodology (extends D-23)
- [ ] **ADR-0014** AI-assisted upstream merge mechanism — adapter usage signatures + confidence scoring + sandbox sim (extends D-17)
- [ ] **ADR-0015** AI-assisted config generation mechanism — NL → schema validation + adapter recommendation + hallucination guardrails (extends D-22)
- [ ] **ADR-0016** AI-streaming UI primitives integration with AI Sub 3 + LLM Gateway (extends D-29)
- [x] **ADR-0017** Status-page adapter pattern + brand-package mechanism + admin-UI adapter (extends D-18, D-19, D-20) — *locked 2026-05-05 as D-49; built-in lightweight + Instatus MVP-1 status page; `@starter-saas/brand` package MVP-1; admin UI adapter contract MVP-1 (impl v1+)*
- [ ] **ADR-0018** Deploy command portal mechanism — local web/TUI server + pause/resume state machine + AI narration prompt design (extends D-43)
- [ ] **ML platform** decision (pluggable / vendor-default / deferred to v2)

### Notes

- Story may split if AC count remains unmanageable. Reasonable split-points: ADR-0004..0007 (core architecture) → STORY-009A; ADR-0008..0012 (AI subsystems) → STORY-009B; ADR-0013..0018 (plugin / AI-mechanisms / cross-cutting) → STORY-009C.
- ADRs may land as `status: proposed` initially and be promoted to `accepted` as designs solidify. Goal is to lock the *direction*, not the implementation detail.
- Story estimate bumped from L → XL to reflect the absorbed ADRs.

## Tasks under this Story

(Tasks created on demand during grooming.)

## Dependencies

- Blocks: STORY-012 (MVP-1 surface)
- Blocked by: STORY-008, STORY-010

## Notes

These decisions interact: tenancy choice constrains event-bus throughput requirements; observability default depends on whether self-hosted-first or vendor-first; plugin architecture depends on whether tenancy is shared-DB or DB-per-tenant. Discuss in dependency order.

## Activity log

- 2026-04-25 — created (Phase B placeholder)
- 2026-05-02 — picked up; STORY-008 + STORY-010 + STORY-011 closed (32 decisions D-12..D-43; 3 ADRs accepted: ADR-0001 license, ADR-0002 tech stack, ADR-0003 cloud target). ACs expanded to absorb the ADR queue accumulated during prior stories — ADR-0004 (multi-tenancy) through ADR-0018 (deploy command portal mechanism). Story estimate bumped L → XL. **Likely-split planned**: STORY-009A core architecture (ADR-0004..0007 + saga + data-quality), STORY-009B AI subsystems (ADR-0008..0012), STORY-009C plugin + AI-mechanisms + cross-cutting (ADR-0013..0018). First question: **ADR-0004 multi-tenancy detail** — per-schema migration runner + cross-schema query primitives + tenant provisioning flow (D-33 already locked schema-per-tenant; this fleshes out the *how*).
- 2026-05-02 — **Pacing strategy locked** via AskUserQuestion: tight bundling (5-7 broader Qs grouping related ADRs) over per-ADR full treatment or split-now.
- 2026-05-02 — **Q1 locked** (ADR-0004 multi-tenancy detail): all 4 sub-areas + 6 side picks per assistant recommendation. Migration runner = `@starter-saas/cli tenant migrate` with default-5 parallelism + continue-on-error. Cross-schema queries = `platform` schemas + `withTenants()` wrapper (federated views rejected). Connection pool = PgBouncer transaction mode + per-tenant 100 q/s rate-limit middleware. Tenant provisioning = 9-step idempotent + compensating saga (drives ADR-0005 event-bus design). Archival = 2-stage (soft archive immediate + 30-day default hard-delete retention) with `legal_hold` flag for GDPR. Side picks: UUIDv7 tenant ID, `tenant_{uuid}` schema names, co-resident `platform` schemas, all configurables tunable in `starter.config.ts`. Logged as **D-44**; **ADR-0004 written and accepted** at `docs/architecture/ADR-0004-multi-tenancy.md`. ACs ticked: ADR-0004 ✓ (1 of ~14). Next: **Q2 — ADR-0005 event bus + saga choreography pattern** (Postgres NOTIFY / Redis Streams / Kafka).
- 2026-05-05 — **Q2 locked** (ADR-0005 event bus + saga choreography). User reframed Kafka from v2+ to **v1 target state** with critical design discipline: bus contract surface must be Kafka-shaped from day 1 so MVP-1 → v1 migration is a drop-in adapter swap. **MVP-1 default**: `@starter-saas/event-bus-pg-outbox` (Kafka semantics over Postgres NOTIFY + outbox table — atomic-with-business-state via same-transaction writes, durable, ordered-per-partition-key, at-least-once). **v1 target**: `@starter-saas/event-bus-kafka` (native Kafka, same contract). Additional v1+: Redis Streams, AWS EventBridge, GCP Pub/Sub. **Saga choreography**: event-driven (no central orchestrator), TS-coded state machines, instance tracking in `platform.saga_instances`, compensating actions registered per step. **Package separation**: `packages/event-bus` (bus core + adapter contracts + pg-outbox) + `packages/saga` (state machine + compensation registry). Side picks: 100ms poll, 5 retry count, `platform.event_dlq` DLQ, 90-day saga retention, required idempotency keys, partition_key = saga_id, 30s step timeout default. Logged as **D-45**; **ADR-0005 written and accepted** at `docs/architecture/ADR-0005-event-bus.md`. ACs ticked: ADR-0005 ✓ + saga pattern documented ✓ (3 of ~14). Next: **Q3 — ADR-0006 observability stack + ADR-0011 LLM Gateway integration** (coupled — D-38 cost dashboards / per-tenant LLM budgets need observability primitives; LLM traces feed observability backend).
- 2026-05-05 — **Q3 locked** with two important user refinements: **(1) Langfuse MVP-1** alongside OTel — runs as second observability head specifically for LLM-app deep-dive (agent flows, LangGraph node tracing, prompt-response capture, eval framework). **(2) LLM Gateway is a policy enforcement point, NOT a routing brain** — provider/model selection happens at the capability/component level (each feature declares its preferred provider + model); Gateway intercepts to apply cross-cutting governance only. **D-46 (observability)**: OTel SDK + GenAI semconv + cloud-native backend MVP-1 (CloudWatch / Cloud Logging per D-41) + Langfuse MVP-1; v1+ adapters Grafana stack / Datadog / New Relic / Honeycomb; cost dashboard MVP-1 (per-provider/tenant/feature) + per-tenant budget enforcement MVP-1 (`platform.tenant_budgets` table; soft 80%, hard cutoff); side picks adaptive sampling (100% errors / 1% healthy), PII scrubbing required by default (Pino redactor + OTel redactor; LLM prompts NOT fully logged), retention 30d/7d/90d. **D-47 (LLM Gateway)**: 4 components — Gateway core (interception + OTel + Langfuse + cost-event emission), Safety layer (PII/PCI/profanity/illegal-advice/prompt-injection/bias filters; configurable per-tenant), Model Hub (registry of providers/models; adopter-hosted custom-model support; provisioned-throughput-request workflow v1+), Eval/Cost (eval framework hooks; per-call cost tracking; per-tenant budget enforcement). **Refines D-35** (Opus 4.7 kit-default) and **D-38** (adopter-config-driven routing): both are now FEATURE-level config, not Gateway-level. **ADR-0006 + ADR-0011 written and accepted**. ACs ticked: ADR-0006 ✓ + ADR-0011 ✓ (5 of ~14). Next: **Q4 — ADR-0007 auth provider + ADR-0017 status-page + brand-package + admin-UI adapters**.
- 2026-05-05 — **Q4 locked** end-to-end per assistant recommendation. **D-48 (auth provider)**: Auth.js v5+ MVP-1 default with Drizzle adapter; auth tables in `platform.{users,sessions,accounts,verification_tokens}`; `platform.user_tenant` for tenant memberships; tenant-switcher UI for multi-tenant users; v1+ adapters Clerk / Auth0 / Cognito / GCP Identity Platform / WorkOS / Authelia / Ory Kratos. Flows MVP-1: email+pwd (bcrypt 12, 12-char min, HIBP breach check opt-in), magic link, OAuth (Google/GitHub/Apple), TOTP 2FA. v1+ flows: SAML SSO, OIDC, passkeys/WebAuthn. RBAC sketch: per-tenant roles in `tenant_xyz.{roles,user_roles}`, seeded on provisioning (admin/member/viewer), Fastify middleware checks (`requireRole`/`requirePermission`); full RBAC ADR deferred. Side picks: DB-backed sessions, 30d rolling + 7d idle, token reset 1h expiry one-time, email verification required for pwd not OAuth, 5-attempt 15-min lockout, audit log to `platform.audit_log`. **D-49 (status/brand/admin-UI)**: Status page MVP-1 = built-in lightweight self-hosted (read-only, auto-updated from D-46 incidents) + Instatus managed adapter; v1+ Atlassian Statuspage / Better Stack / Cachet. Brand package `@starter-saas/brand` MVP-1 with logo+colors+typography+copy+OG images; Tailwind config consumes tokens; adopter customization via fork OR `starter.config.ts → brand: {...}` OR `brand: { mode: "raw" }` for direct HTML/CSS. Admin UI adapter CONTRACT MVP-1 (no impl until v1+): defines `AdminUIAdapter` interface for branding/flags/tenants/RBAC/observability surfaces; engineer-only config (stack/cloud/adapters) NEVER in admin UI; always RBAC-gated to `platform_admin` role. **ADR-0007 + ADR-0017 written and accepted**. ACs ticked: ADR-0007 ✓ + ADR-0017 ✓ (7 of ~14). Next: **Q5 — AI subsystems batch (ADR-0008 Customer Profile Builder + ADR-0009 Context-Resolving Query + ADR-0010 Agent Platform + ADR-0012 AI Coworker for Internal Ops)**.
- 2026-05-05 — **Q5 locked** end-to-end per assistant recommendation. 4 AI subsystem ADRs written and accepted. **D-50 (ADR-0008 Sub 1 Customer Profile Builder + AI-Native Stores)**: foundation MVP-1 (event bus + pgvector + LLM Gateway), full subsystem v1; event-bus consumer per-tenant; 5 default profile types (usage, business, interaction, session, feature) with adopter-extensible custom types; streaming incremental updates with configurable embedding triggers; re-embedding migration tool v1; multi-tenant isolation by construction. **D-51 (ADR-0009 Sub 2 Context-Resolving Query Service)**: v1 (foundation MVP-1); 3-layer pipeline plan/execute/synthesize; planner LLM = Sonnet 4.6 default per D-47; permission filter at execute layer (cross-tenant leakage blocked by construction); plan cache by `(tenant, query_hash, schema_version)`; result cache by `(tenant, query_hash, 5-min-bucket)`; schema introspection feeds planner; audit log to `platform.context_query_audit`. **D-52 (ADR-0010 Sub 3 Agent Platform + Omnichannel Orchestrator)**: MVP-1 subset = web surface + 4 of 6 registries (skills, tools, AI-native widgets, UI shell); LangGraph-style decision graph; v1 = subagent registry + non-UI embed + mobile (RN); v1+ = telephonic (Twilio/Vonage) + advanced orchestration; per-tenant agent_config; channel adapter pattern; conversation state in tenant schemas; tool execution under agent's RBAC. **D-53 (ADR-0012 Sub 5 AI Coworker for Internal Ops)**: MVP-1 lite = D-17/D-22/D-23 (already locked); v2 full = conversational + workflows + scheduled tasks + knowledge synthesis; **architecture registry MVP-1** (`platform.architecture_registry` auto-populated from package graph + Zod schemas + ADRs); skills + tool registry v2; `platform_admin` only RBAC; integrates with D-43 deploy portal for AI narration. **ADR-0008 + ADR-0009 + ADR-0010 + ADR-0012 written and accepted**. ACs ticked: ADR-0008 + ADR-0009 + ADR-0010 + ADR-0012 ✓ (11 of ~14). Next: **Q6 closes STORY-009** with cross-cutting + AI-mechanism ADRs (ADR-0013 plugin spec + ADR-0014 AI-assisted merge + ADR-0015 AI-assisted config gen + ADR-0016 ai-ui integration + ADR-0018 deploy portal mechanism) + ML platform decision + data quality decision.
